#include "holberton.h"

/**
* _abs - absolute value of an integer.
* @a: is the value will go to evaluate
* Return: if condition is true ? the value -a : otherwise a.
*/

int _abs(int a)
{
	return ((a < 0) ? (-a) : (a));
}
