#include "holberton.h"

/**
* _isupper - check the variable is UPPERCASE.
* @c: is the variable.
* Return: Always 0.
*/

int _isupper(int c)
{
	if (c == 'a')
	{
		return (0);
	}
	else
	{
		return (1);
	}
	return (0);
}
