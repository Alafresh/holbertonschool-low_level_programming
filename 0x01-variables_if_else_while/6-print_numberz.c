#include <stdio.h>
/**
* main - prints the alphabet in lowercase, and then in uppercase.
*
* Return: take an array from char
*/
int main(void)
{
int a;
for (a = '0'; a <= '9'; a++)
putchar(a);
putchar('\n');
return (0);
}
