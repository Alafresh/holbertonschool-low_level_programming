#include <stdio.h>
/**
 * main - check the last number is greater than 5,less than 6 or zero
 *
 * Return: take an array from char
 */
int main(void)
{
char a;
a = 'a';
while (a <= 'z')
{
putchar(a);
a++;
}
putchar('\n');
return (0);
}
